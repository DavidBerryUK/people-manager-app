import React from "react";

const LayoutListRegionListDetailContainer: React.FC = (props) => {
  return <div className="region-list-detail-container">{props.children}</div>;
};

export default LayoutListRegionListDetailContainer;
